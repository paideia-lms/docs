---
type: tutorial
title: Prepare your environment
description: |-
  Tutorial: Getting Started with Paideia LMS —
  Install the prerequisites and tools you'll need to complete the tutorial
i18nReady: true
---
import Checklist from '~/components/Checklist.astro';
import Box from '~/components/tutorial/Box.astro';
import MultipleChoice from '~/components/tutorial/MultipleChoice.astro';
import Option from '~/components/tutorial/Option.astro';
import PreCheck from '~/components/tutorial/PreCheck.astro';
import { Steps } from '@astrojs/starlight/components';
import Badge from "~/components/Badge.astro"

<PreCheck>
  - Install any tools and set up prerequisites that you will use to run Paideia LMS
</PreCheck>

## Get the prerequisites you need

### Operating System

Paideia LMS runs on **macOS** (ARM64 or x64) or **Linux** (ARM64 or x64). Make sure you have access to a compatible operating system.

### Terminal

You will use a **command line (terminal)** to download, install, and run Paideia LMS, and to configure your environment.

You can access the command line through a local terminal program for your operating system. Common applications include **Terminal** (macOS/Linux), **Command Prompt** (Windows), and **Termux** (Android). One of these will probably already be on your machine.

### PostgreSQL Database

Paideia LMS requires a **PostgreSQL** database. You'll need either:
- A local PostgreSQL installation
- Access to a remote PostgreSQL database
- Docker Compose (which includes PostgreSQL)

To check if you have PostgreSQL installed locally, run:

```sh
psql --version

# Example output
psql (PostgreSQL) 18.0
```

If you don't have PostgreSQL installed, you can:
- Install PostgreSQL locally
- Use Docker Compose (which we'll cover in the installation steps)
- Use a cloud PostgreSQL service

### S3-Compatible Storage

Paideia LMS requires **S3-compatible storage** for file storage. You'll need either:
- MinIO (S3-compatible storage) - included with Docker Compose
- An S3-compatible service (AWS S3, DigitalOcean Spaces, etc.)
- Local MinIO installation

### Code Editor (Optional)

While not required, a **code editor** can be helpful for editing configuration files. 

:::tip[We'll use...]
This tutorial will use **VS Code**, but you can use any editor for your operating system.
:::

<Steps>
1. [Download and install VS Code](https://code.visualstudio.com/#alt-downloads) or another code editor of your choice (optional).
</Steps>

<Box icon="question-mark">

### Test your knowledge

Which of the following is...
1. A database system required by Paideia LMS?

    <MultipleChoice>
      <Option>
        MySQL
      </Option>
      <Option isCorrect>
        PostgreSQL
      </Option>
      <Option>
        MongoDB
      </Option>
    </MultipleChoice>

2. Storage system required by Paideia LMS?

    <MultipleChoice>
      <Option>
        Local file system
      </Option>
      <Option isCorrect>
        S3-compatible storage
      </Option>
      <Option>
        Google Drive
      </Option>
    </MultipleChoice>

3. An application for running commands?
    <MultipleChoice>
      <Option>
        VS Code
      </Option>
      <Option isCorrect>
        Terminal
      </Option>
      <Option>
        Web browser
      </Option>
    </MultipleChoice>

</Box>

<Box icon="check-list">
## Checklist for moving on

<Checklist>
- [ ] I can access the command line in a terminal.
- [ ] I have PostgreSQL available (or will use Docker Compose).
- [ ] I have S3-compatible storage available (or will use Docker Compose).
- [ ] I have a code editor like VS Code (optional).
</Checklist>
</Box>

### Resources

- <p>[PostgreSQL Documentation](https://www.postgresql.org/docs/) <Badge class="neutral-badge" text="external" /> — official PostgreSQL documentation</p>
- <p>[MinIO Documentation](https://min.io/docs/) <Badge class="neutral-badge" text="external" /> — MinIO S3-compatible storage documentation</p>
